apiVersion: v1
kind: Service
metadata:
  name: ai-bot-frontend
  namespace: ai-bot
spec:
  selector:
    app: ai-bot-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP